# Install-integrity experiment — results

Generated by `run.py`. Validates CS-006 (install-source guard) and CS-007
(download verification) from ADR-0002. `vulnerable` = current install.sh
logic; `fixed` = proposed logic in `candidates/fixed.sh`.

- Legit statusline SHA-256 (the release pin): `17fde2d087eeaf4d4d0a66e7160b7fb8693896b10f0fa912f9e9fd2d1efdb0dd`
- Fixed-candidate failures: **0**

| Scenario | Candidate | Safe | Detail |
| --- | --- | :---: | --- |
| cwd-hijack (pipe, malicious file in cwd, honest server) | `vulnerable` | ❌ | installed the ATTACKER file |
| cwd-hijack (pipe, malicious file in cwd, honest server) | `fixed` | ✅ | installed the legit copy |
| honest download (pipe, empty cwd, honest server) | `vulnerable` | ✅ | installed the legit copy |
| honest download (pipe, empty cwd, honest server) | `fixed` | ✅ | installed the legit copy |
| tampered download (pipe, empty cwd, server bytes != pin) | `vulnerable` | ❌ | installed the TAMPERED download |
| tampered download (pipe, empty cwd, server bytes != pin) | `fixed` | ✅ | aborted (rc=4), no bad file installed |
| clone install (clone mode, legit file beside script) | `vulnerable` | ✅ | installed the legit copy |
| clone install (clone mode, legit file beside script) | `fixed` | ✅ | installed the legit copy |

## Reading

- **cwd-hijack**: the current logic installs a `statusline.py` planted in the
  working directory (`❌ vulnerable`); the fix ignores cwd and downloads the
  verified copy.
- **tampered download**: the fix aborts non-zero when downloaded bytes don't
  match the pinned digest; the current logic installs whatever it fetched.
- **clone install**: both install the legit local copy — the fix does not
  regress the clone-and-run path.

Note: `fixed.sh` fails **closed** (exit 3) when no `shasum`/`sha256sum` is
present rather than skipping verification — see ADR-0002 D2.
